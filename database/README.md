# Runtime Database Storage

This folder contains the live SQLite data and operational files.

- `telecom_forensics.db` — live database
- `backups/` — database backups
- `import_logs/` — CGI import logs

Python source code is stored in `modules/database/`.
